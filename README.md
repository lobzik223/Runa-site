# # RUNA Finance Website

Современный веб-сайт для RUNA Finance с адаптивным дизайном и готовностью к интеграции с бэкендом.

## 🚀 Возможности

- ✅ Полностью адаптивный дизайн для всех устройств
- ✅ Автоматическое воспроизведение видео без контролов
- ✅ Оптимизация для мобильных устройств
- ✅ Готовая структура для интеграции API
- ✅ TypeScript для безопасности типов
- ✅ Современный стек технологий

## 🛠️ Технологии

- React 18
- TypeScript
- Vite
- CSS3

## 📦 Установка

```bash
# Установка зависимостей
npm install
```

## 🎯 Запуск проекта

### Режим разработки

```bash
npm run dev
```

Сайт будет доступен по адресу: http://localhost:3000

### Сборка для продакшена

```bash
npm run build
```

### Предпросмотр продакшен сборки

```bash
npm run preview
```

## 📱 Адаптивность

Сайт полностью адаптирован для всех устройств:
- 💻 Десктоп (1920px+)
- 💻 Ноутбуки (1366px - 1920px)
- 📱 Планшеты (768px - 1366px)
- 📱 Мобильные устройства (320px - 768px)

## 🎥 Видео

Видео автоматически воспроизводится при загрузке страницы без отображения элементов управления плеера. Поддерживаются форматы:
- .mov (QuickTime)
- .mp4 (MPEG-4)
- .webm (WebM)

## 🔌 Интеграция API

Проект готов к подключению бэкенда. Подробная инструкция находится в файле [API_INTEGRATION.md](./API_INTEGRATION.md).

### Быстрый старт API

1. Создайте файл `.env`:
```env
VITE_API_URL=http://localhost:3001/api
```

2. Используйте API сервис:
```typescript
import { api } from './services/api';

const data = await api.get('/endpoint');
```

## 📁 Структура проекта

```
Runa-site/
├── src/
│   ├── components/      # React компоненты
│   │   ├── Header.tsx
│   │   ├── HeroSection.tsx
│   │   └── images/
│   ├── services/        # API сервисы
│   │   ├── api.ts
│   │   └── types.ts
│   ├── config/          # Конфигурация
│   │   └── api.config.ts
│   ├── mov/             # Видео файлы
│   ├── App.tsx
│   ├── App.css
│   ├── index.css
│   └── main.tsx
├── public/              # Статические файлы
├── package.json
├── vite.config.ts
└── tsconfig.json
```

## 🐛 Решение проблем

### Видео не воспроизводится

1. Убедитесь, что видео файл находится в `src/mov/`
2. Проверьте формат видео (поддерживаются .mov, .mp4, .webm)
3. Очистите кеш браузера

### Ошибка 404

1. Убедитесь, что все файлы на месте
2. Пересоберите проект: `npm run build`
3. Проверьте правильность путей импорта

### Проблемы с мобильной версией

1. Очистите кеш браузера на мобильном устройстве
2. Проверьте, что viewport мета-тег установлен правильно
3. Убедитесь, что используется последняя версия браузера

## 📝 Дальнейшее развитие

- [ ] Добавить секции: Projects, Team, Clients, About
- [ ] Интегрировать с бэкендом API
- [ ] Добавить анимации при прокрутке
- [ ] Реализовать многоязычность
- [ ] Добавить форму обратной связи

## 📄 Лицензия

ISC

## 👥 Поддержка

При возникновении вопросов или проблем создайте issue в репозитории.

Сайт для RUNA Finance приложения.

## Установка

```bash
npm install
```

## Запуск

```bash
npm run dev
```

Сайт будет доступен по адресу: http://localhost:3000

## Сборка

```bash
npm run build
```

## Запуск production сервера

```bash
npm start
```
